# --- Dependencies ---
node_modules/
package-lock.json   # Optional: keep this if you want to lock versions, ignore if using yarn exclusively
yarn.lock           # Optional: same as above
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- Environment Variables (CRITICAL SECURITY) ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# --- Operating System Files ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# ==========================
# BACKEND (Node.js/Express)
# ==========================
logs
*.log
npm-debug.log*
coverage/
dist/
build/

# ==========================
# MOBILE CLIENT (React Native / Expo)
# ==========================

# Expo specific
.expo/
web-build/

# Android build files
android/app/build/
android/app/src/main/assets/
android/app/src/main/res/
android/app/google-services.json  # Security: Don't upload Firebase keys
*.keystore                        # Security: Don't upload release keys!
*.jks                             # Security: Don't upload release keys!

# iOS build files
ios/*.xcworkspace
ios/Pods/
ios/.xcode.env.local
ios/Podfile.lock
ios/build/
ios/DerivedData/

# Metro Bundler
.metro-health-check

# Editors / IDEs
.idea/
.vscode/
*.swp
*.swo